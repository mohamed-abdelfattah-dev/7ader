import 'package:flutter/material.dart';

class AppColors {
  // Brand Colors
  static const Color primary = Color(0xFF6A1B9A); // Modern Purple
  static const Color primaryDark = Color(
    0xFF4A148C,
  ); // Darker shade for contrast
  static const Color secondary = Color(0xFF9C27B0); // Secondary accent

  // Background Surfaces
  static const Color background = Color(0xFFF2F2F2); // Light Gray Background
  static const Color surface = Colors.white; // Card/Sheet Background
  static const Color scaffoldBackground = Color(0xFFF5F5F8);

  // Typography
  static const Color textPrimary = Color(0xFF000000); // Main Text
  static const Color textSecondary = Color(0xFF9A9A9D); // Subtitles/Hints
  static const Color textInverse = Colors.white;

  // Status Colors
  static const Color error = Color(0xFFD32F2F);
  static const Color success = Color(0xFF388E3C);
  static const Color warning = Color(0xFFFBC02D);

  // UI Elements
  static const Color inputBorder = Color(0xFFBDBDBD);
  static const Color divider = Color(0xFFEEEEEE);
  static const Color shadow = Color(0x33000000);
}
